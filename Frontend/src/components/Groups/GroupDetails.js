import React from 'react';

const GroupDetails = ({ group }) => {
    // Details of a single group
    return (
        <div>
            {/* Display the details of the group here */}
        </div>
    );
};

export default GroupDetails;